//brushfire

#include <stdio.h>

/*
#include "eyebot.h"
#include "image.c"
#include <stdbool.h>
#include <stdlib.h>
#include <math.h>
#include <stdbool.h>
#include <windef.h>

BYTE image[128][128];
int width=128;
int height=128;

bool field[128][128];
int grid[128][128];

void floodfill(int i,int j) {
    for (int a=i;a<width;a++){
        for (int b=j;b<height;b++){

        }
    }
}


void findconnectedcomponents()  {
    int nblocks=0;
    for (int i=0;i<width;i++ ) {
        for (int j=0;j<height;j++) {
            if (field[i][j]) {
                floodfill(i,j);
                //find all connected components.. floodfill?
                grid[i][j].color=000000;
                grid[i][j].value=1;
                nblocks++;
            }
        }
    }
}

void firstbrushfire(){
    //do first iteration
}

void brushfire() {
    for (int i=0;i<width;i++ ) {
        for (int j =0;j<height;j++) {
            iteration=
            if (grid[i][j].value==iteration-1) {
                for

            }
        }
    }
}
*/
int main() {
/*
    char *filename="fourblocks.pbm";
    BYTE *img;
    read_pbm(filename,&img);
    LCDArea(0,0,128,128,0xFFFFFF,1);
    LCDImageStart(0,0,128,128);
    LCDImageBinary(img);
    OSWait(9000);

    for (int i=0;i<128;i++) {
        for (int j=0;j<128;j++) {
            field[j][i]=img[i*128+j];
        }
    }
*/

    int array[2][2];

    printf("%d,%d\n%d,%d\n",array[0][2],array[0][1],array[1][0],array[1][1]);
    return 0;
}